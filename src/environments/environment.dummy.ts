/*
    This is a dummy file to represent the files you need to make the project work as intended.

    Instead of this file you need two files called
        - environment.ts
        - environment.prod.ts

    Also don't forget to add key for OpenWeatherMap

    googleAutocomplete_key is meant to be used in index.html in the js script.

*/
export const environment = {
    production: false,
    owmKey: 'Api key to OpenWeatherMap',
    // Google api keys might be the same, copy the code to both if so
    googleAutocomplete_key: 'Api key to Google javascript api',
    googleGeocode_key: 'Api key for Google Geocode api'
};
